import { ScheduleType } from '../../../types/schedule.type';

interface ScheduleProps {
  schedule: ScheduleType;
}

export const TitlesSchedule = ({ schedule }: ScheduleProps) => {
  return <div>TitlesSchedule</div>;
};
